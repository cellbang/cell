{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "malagu",
            "program": "${workspaceRoot}/node_modules/@malagu/cli/bin/malagu-serve",
            "cwd": "${workspaceFolder}/browser-app"
        }
    ]
}