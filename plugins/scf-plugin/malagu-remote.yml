mode: "${'api-gateway' in currentMode ? 'api-gateway-mode' : 'timer' in currentMode ? 'timer-mode' : 'apigw-basic' in currentMode ? 'tigger-apigw-mode' : 'http-mode'}"
malagu:
  cloud:
    namespace:
      name: default
    function:
      handler: index.handler
      namespace: ${malagu.cloud.namespace.name}
      runtime: Nodejs12.16
