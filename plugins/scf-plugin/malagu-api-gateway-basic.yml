malagu:
  cloud:
    trigger:
      name: ${malagu.cloud.function.name}
      type: "apigw"
      enable: "OPEN"
      triggerDesc: 
        service: 
          serviceType: "BASIC"
        api: 
          requestConfig:
            path: '/'
        release:
          environmentName: ${malagu.cloud.alias.name}