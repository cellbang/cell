{
  "private": true,
  "name": "@malagu/example-docker-app",
  "keywords": [
    "malagu-component"
  ],
  "version": "2.42.0",
  "license": "MIT",
  "files": [
    "lib",
    "src"
  ],
  "dependencies": {
    "@malagu/core": "2.42.0",
    "@malagu/react": "2.42.0",
    "@malagu/rpc": "2.42.0",
    "@malagu/serve-static": "2.42.0",
    "react": "^18.2.0"
  },
  "devDependencies": {
    "@malagu/cli": "2.42.0"
  },
  "scripts": {
    "clean": "rimraf lib dist .malagu",
    "build": "malagu build",
    "start": "malagu serve",
    "deploy": "malagu deploy -m scf -m test",
    "deploy:test": "malagu deploy -m scf -m test",
    "deploy:pre": "malagu deploy -m scf -m pre",
    "deploy:prod": "malagu deploy -m scf -m prod"
  }
}
