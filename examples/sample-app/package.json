{
  "private": true,
  "name": "@celljs/example-sample-app",
  "keywords": [
    "cell-component"
  ],
  "version": "3.5.7",
  "license": "MIT",
  "files": [
    "lib",
    "src"
  ],
  "dependencies": {
    "@celljs/core": "3.5.7",
    "@celljs/react": "3.5.7",
    "@celljs/rpc": "3.5.7",
    "@celljs/serve-static": "3.5.7",
    "react": "^18.2.0"
  },
  "devDependencies": {
    "@celljs/cli": "3.5.7",
    "@celljs/cli-service": "3.5.7"
  },
  "scripts": {
    "clean": "rimraf lib dist .cell",
    "build": "cell build",
    "start": "cell serve",
    "deploy": "cell deploy -m scf -m test",
    "deploy:test": "cell deploy -m scf -m test",
    "deploy:pre": "cell deploy -m scf -m pre",
    "deploy:prod": "cell deploy -m scf -m prod"
  }
}
