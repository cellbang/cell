{
  "name": "@celljs/example-sample-app",
  "version": "3.6.2",
  "private": true,
  "keywords": [
    "cell-component"
  ],
  "license": "MIT",
  "files": [
    "lib",
    "src"
  ],
  "scripts": {
    "build": "cell build",
    "clean": "rimraf lib dist .cell",
    "deploy": "cell deploy -m scf -m test",
    "deploy:pre": "cell deploy -m scf -m pre",
    "deploy:prod": "cell deploy -m scf -m prod",
    "deploy:test": "cell deploy -m scf -m test",
    "start": "cell serve"
  },
  "dependencies": {
    "@celljs/core": "3.6.2",
    "@celljs/react": "3.6.2",
    "@celljs/rpc": "3.6.2",
    "@celljs/serve-static": "3.6.2",
    "react": "^18.2.0"
  },
  "devDependencies": {
    "@celljs/cli": "3.6.2",
    "@celljs/cli-service": "3.6.2"
  }
}
