{
  "private": true,
  "name": "@celljs/example-sample-app",
  "keywords": [
    "cell-component"
  ],
  "version": "3.5.8",
  "license": "MIT",
  "files": [
    "lib",
    "src"
  ],
  "dependencies": {
    "@celljs/core": "3.5.8",
    "@celljs/react": "3.5.8",
    "@celljs/rpc": "3.5.8",
    "@celljs/serve-static": "3.5.8",
    "react": "^18.2.0"
  },
  "devDependencies": {
    "@celljs/cli": "3.5.8",
    "@celljs/cli-service": "3.5.8"
  },
  "scripts": {
    "clean": "rimraf lib dist .cell",
    "build": "cell build",
    "start": "cell serve",
    "deploy": "cell deploy -m scf -m test",
    "deploy:test": "cell deploy -m scf -m test",
    "deploy:pre": "cell deploy -m scf -m pre",
    "deploy:prod": "cell deploy -m scf -m prod"
  }
}
