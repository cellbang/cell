{
  "private": true,
  "name": "@celljs/example-sample-app",
  "keywords": [
    "cell-component"
  ],
  "version": "2.60.3",
  "license": "MIT",
  "files": [
    "lib",
    "src"
  ],
  "dependencies": {
    "@celljs/core": "2.60.3",
    "@celljs/react": "2.60.3",
    "@celljs/rpc": "2.60.3",
    "@celljs/serve-static": "2.60.3",
    "react": "^18.2.0"
  },
  "devDependencies": {
    "@celljs/cli": "2.60.3",
    "@celljs/cli-service": "2.60.3"
  },
  "scripts": {
    "clean": "rimraf lib dist .cell",
    "build": "cell build",
    "start": "cell serve",
    "deploy": "cell deploy -m scf -m test",
    "deploy:test": "cell deploy -m scf -m test",
    "deploy:pre": "cell deploy -m scf -m pre",
    "deploy:prod": "cell deploy -m scf -m prod"
  }
}
