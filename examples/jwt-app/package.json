{
  "name": "@celljs/example-jwt-app",
  "version": "3.6.4",
  "private": true,
  "keywords": [
    "cell-component"
  ],
  "license": "MIT",
  "files": [
    "lib",
    "src"
  ],
  "scripts": {
    "build": "cell build",
    "clean": "rimraf lib dist .cell",
    "deploy": "cell deploy -m scf -m test",
    "deploy:pre": "cell deploy -m scf -m pre",
    "deploy:prod": "cell deploy -m scf -m prod",
    "deploy:test": "cell deploy -m scf -m test",
    "start": "cell serve",
    "test": "cell-component test"
  },
  "dependencies": {
    "@celljs/core": "3.6.4",
    "@celljs/jwt": "3.6.4",
    "@celljs/mvc": "3.6.4",
    "@celljs/web": "3.6.4"
  },
  "devDependencies": {
    "@celljs/cli": "3.6.4",
    "@celljs/component": "3.6.4",
    "@celljs/testing": "3.6.4",
    "@types/supertest": "^2.0.12",
    "supertest": "^6.2.4"
  }
}
