{
  "private": true,
  "name": "@celljs/example-security-app",
  "keywords": [
    "cell-component"
  ],
  "version": "2.60.3",
  "license": "MIT",
  "files": [
    "lib",
    "src"
  ],
  "dependencies": {
    "@celljs/core": "2.60.3",
    "@celljs/mvc": "2.60.3",
    "@celljs/security": "2.60.3"
  },
  "devDependencies": {
    "@celljs/cli": "2.60.3"
  },
  "scripts": {
    "clean": "rimraf lib dist .cell",
    "build": "cell build",
    "start": "cell serve",
    "deploy": "cell deploy -m scf -m test",
    "deploy:test": "cell deploy -m scf -m test",
    "deploy:pre": "cell deploy -m scf -m pre",
    "deploy:prod": "cell deploy -m scf -m prod"
  }
}
