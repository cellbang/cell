{
  "name": "@celljs/example-security-app",
  "version": "3.6.15",
  "private": true,
  "keywords": [
    "cell-component"
  ],
  "license": "MIT",
  "files": [
    "lib",
    "src"
  ],
  "scripts": {
    "build": "cell build",
    "clean": "rimraf lib dist .cell",
    "deploy": "cell deploy -m scf -m test",
    "deploy:pre": "cell deploy -m scf -m pre",
    "deploy:prod": "cell deploy -m scf -m prod",
    "deploy:test": "cell deploy -m scf -m test",
    "start": "cell serve"
  },
  "dependencies": {
    "@celljs/core": "3.6.15",
    "@celljs/mvc": "3.6.15",
    "@celljs/security": "3.6.15"
  },
  "devDependencies": {
    "@celljs/cli": "3.6.15"
  }
}
