mode: [ database, oauth2.0, locale, theme ]

accounts:
  home:
    url: https://www.cellbang.com
  logo:
    icon: Cellbang
frontend:
  malagu:
    security:
      loginPage: /login
backend:
  malagu:
    serve-static:
      apiPath: "*oauth2/*"  # 确保 oauth2.0 相关的 url 不会被 server-static 组件识别为静态文件地址