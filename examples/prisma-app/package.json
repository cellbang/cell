{
  "name": "@celljs/example-prisma-app",
  "version": "3.6.9",
  "private": true,
  "keywords": [
    "cell-component"
  ],
  "license": "MIT",
  "files": [
    "lib",
    "src"
  ],
  "scripts": {
    "build": "cell build -m scf",
    "clean": "rimraf lib dist .cell",
    "deploy": "cell deploy -m scf -m test",
    "deploy:pre": "cell deploy -m scf -m pre",
    "deploy:prod": "cell deploy -m scf -m prod",
    "deploy:test": "cell deploy -m scf -m test",
    "start": "cell serve",
    "test": "cell-component test"
  },
  "dependencies": {
    "@celljs/core": "3.6.9",
    "@celljs/mvc": "3.6.9",
    "@prisma/client": "^4.4.0",
    "prisma": "^4.4.0"
  },
  "devDependencies": {
    "@celljs/cli": "3.6.9",
    "@celljs/cli-service": "3.6.9",
    "@celljs/component": "3.6.9",
    "@celljs/testing": "3.6.9",
    "@types/supertest": "^2.0.12",
    "supertest": "^6.2.4"
  }
}
