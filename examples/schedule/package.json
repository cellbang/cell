{
  "name": "@celljs/example-schedule",
  "version": "3.7.0",
  "private": true,
  "keywords": [
    "cell-component"
  ],
  "license": "MIT",
  "files": [
    "lib",
    "src"
  ],
  "scripts": {
    "build": "cell build",
    "clean": "rimraf lib dist .cell",
    "deploy": "cell deploy -m scf -m test",
    "deploy:pre": "cell deploy -m scf -m pre",
    "deploy:prod": "cell deploy -m scf -m prod",
    "deploy:test": "cell deploy -m scf -m test",
    "start": "cell serve"
  },
  "dependencies": {
    "@celljs/core": "3.7.0",
    "@celljs/schedule": "3.7.0",
    "@celljs/web": "3.7.0"
  },
  "devDependencies": {
    "@celljs/cli": "3.7.0",
    "@celljs/dev-server": "3.7.0"
  }
}
