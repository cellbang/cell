{
  "private": true,
  "name": "@malagu/example-schedule",
  "keywords": [
    "malagu-component"
  ],
  "version": "2.0.5",
  "license": "MIT",
  "files": [
    "lib",
    "src"
  ],
  "dependencies": {
    "@malagu/core": "2.0.5",
    "@malagu/scf-adapter": "2.0.5",
    "@malagu/schedule": "2.0.5"
  },
  "devDependencies": {
    "@malagu/cli": "2.0.5",
    "@malagu/cli-service": "2.0.5",
    "@malagu/scf-plugin": "2.0.5"
  },
  "scripts": {
    "clean": "rimraf lib dist .malagu",
    "build": "malagu build",
    "start": "malagu serve",
    "deploy": "malagu deploy -m test",
    "deploy:test": "malagu deploy -m test",
    "deploy:pre": "malagu deploy -m pre",
    "deploy:prod": "malagu deploy -m prod"
  }
}
