{
  "private": true,
  "name": "@celljs/example-schedule",
  "keywords": [
    "cell-component"
  ],
  "version": "3.5.13",
  "license": "MIT",
  "files": [
    "lib",
    "src"
  ],
  "dependencies": {
    "@celljs/core": "3.5.13",
    "@celljs/schedule": "3.5.13",
    "@celljs/web": "3.5.13"
  },
  "devDependencies": {
    "@celljs/cli": "3.5.13"
  },
  "scripts": {
    "clean": "rimraf lib dist .cell",
    "build": "cell build",
    "start": "cell serve",
    "deploy": "cell deploy -m scf -m test",
    "deploy:test": "cell deploy -m scf -m test",
    "deploy:pre": "cell deploy -m scf -m pre",
    "deploy:prod": "cell deploy -m scf -m prod"
  }
}
