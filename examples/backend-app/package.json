{
  "name": "@celljs/example-backend-app",
  "version": "3.7.1",
  "private": true,
  "keywords": [
    "cell-component"
  ],
  "license": "MIT",
  "files": [
    "lib",
    "src"
  ],
  "scripts": {
    "build": "cell build",
    "clean": "rimraf lib dist .cell",
    "deploy": "cell deploy -m scf -m test",
    "deploy:pre": "cell deploy -m scf -m pre",
    "deploy:prod": "cell deploy -m scf -m prod",
    "deploy:test": "cell deploy -m scf -m test",
    "start": "cell serve",
    "test": "cell-component test"
  },
  "dependencies": {
    "@celljs/core": "3.7.1",
    "@celljs/mvc": "3.7.1"
  },
  "devDependencies": {
    "@celljs/cli": "3.7.1",
    "@celljs/component": "3.7.1",
    "@celljs/dev-server": "3.7.1",
    "@celljs/testing": "3.7.1",
    "@types/supertest": "^2.0.12",
    "supertest": "^6.2.4"
  }
}
