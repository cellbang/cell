targets: [ 'frontend', 'backend' ]
mode: "${'fc' in currentMode ? 'fc-adapter' : 'lambda' in currentMode ? 'lambda-adapter' : 'server' in currentMode ? 'server-adapter' : 'vercel' in currentMode ? 'vercel-adapter' : 'scf' in currentMode ? 'scf-adapter' : '' }"
