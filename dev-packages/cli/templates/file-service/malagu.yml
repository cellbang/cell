targets:
  - backend
# malagu:
  # cloud:
    # client:
      # 本地测试需要打开，通过公网访问对象存储
      # internal: false
    # 如果不填写 region 和 credentials，自动使用函数所在的 region，以及服务角色自动生成的临时 AK，该服务角色需要拥有访问以下 bucket 的读权限
    # region: cn-hangzhou 
    # credentials:
    #   accessKeyId: xxxxxxx
    #   accessKeySecret: xxxxxx
app:
  bucket: malagu-bucket # 指定你自己的 bucket
