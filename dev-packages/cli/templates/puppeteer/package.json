{
  "name": "puppeteer",
  "keywords": [
    "cell-component"
  ],
  "version": "0.0.0",
  "license": "MIT",
  "files": [
    "lib",
    "src"
  ],
  "dependencies": {
    "@celljs/core": "{{ version }}",
    "@celljs/mvc": "{{ version }}",
    "@celljs/web": "{{ version }}",
    "@celljs/puppeteer": "{{ version }}",
    "@celljs/oss": "{{ version }}"
  },
  "devDependencies": {
    "@celljs/cli": "{{ version }}"
  },
  "scripts": {
    "clean": "rimraf lib dist .cell",
    "build": "cell build",
    "start": "cell serve",
    "deploy": "cell deploy -m fc -m test",
    "deploy:test": "cell deploy -m fc -m test",
    "deploy:pre": "cell deploy -m fc -m pre",
    "deploy:prod": "cell deploy -m fc -m prod"
  }
}
